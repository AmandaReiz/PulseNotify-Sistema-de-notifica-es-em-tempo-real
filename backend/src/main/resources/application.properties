spring.application.name=notifications
server.port=${PORT:8080}

spring.data.redis.url=${REDIS_URL:redis://localhost:6379}

app.cors.allowed-origins=${ALLOWED_ORIGINS:http://127.0.0.1:5173,http://localhost:5173,https://frontend-pi-seven-55.vercel.app}
app.notifications.recent-key=notifications:recent
app.notifications.recent-limit=20
